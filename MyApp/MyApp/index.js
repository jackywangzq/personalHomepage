/**
 * @format
 */

import {AppRegistry} from 'react-native';
import CenterScreen from './src/pages/Login';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => CenterScreen);
